@model IEnumerable<Domain.ProjectStatusReportComment>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ProjectStatusReport.ProjectStatus)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ProjectStatusComment)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ProjectStatusReport.ProjectStatus)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ProjectStatusComment)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ProjectStatusReportCommentId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ProjectStatusReportCommentId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ProjectStatusReportCommentId })
        </td>
    </tr>
}

</table>
