@model IEnumerable<Domain.UserMessage>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.UserMessageTitle)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UserMessageText)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.UserMessageTitle)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.UserMessageText)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.UserMessageId }) |
            @Html.ActionLink("Details", "Details", new { id=item.UserMessageId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.UserMessageId })
        </td>
    </tr>
}

</table>
