@model IEnumerable<Domain.TagType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TagTypeValue)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TagTypeValue)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TagTypeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.TagTypeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TagTypeId })
        </td>
    </tr>
}

</table>
